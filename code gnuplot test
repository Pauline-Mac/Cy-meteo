code barre d'erreur:

set output 'error.png'
Shadecolor = "#80E0A080"
plot 'errorbars.txt' using 1:4:3 with filledcurve fc rgb Shadecolor title "Shaded error region",'' using 1:2 smooth mcspline lw 2 title "Monotonic spline through data"

code carte interpolée:

set pm3d map
set palette color positive
set interpolate 0,0
splot 'moistOutput.txt'

code ligne simple:

plot 't2Output.txt' using 1:2 smooth mcspline lw 2


format type dans fichier sh (remplacer les "  " ):

#!/bin/bash

echo > gnuplot.in 
echo "set terminal png" >> gnuplot.in
echo "set output 'sinuuuss.png'" >> gnuplot.in
echo "plot sin(x)" >> gnuplot.in
gnuplot gnuplot.in
xdg-open sin.png
